VITE_API_URL=http://localhost:8000

# Production (Vercel) — REQUIRED for upload/chat to work:
# VITE_API_URL=https://your-backend-name.onrender.com
#
# After setting on Vercel, you MUST redeploy (Vite bakes env vars at build time).
# See DEPLOYMENT.md for full setup guide.
